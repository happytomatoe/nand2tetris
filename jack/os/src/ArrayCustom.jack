class ArrayCustom {

    /** Constructs a new Array of the given size. */
    function Array new(int size) {
        return MemoryCustom.alloc(size); 
    }

    /** Disposes this array. */
    method void dispose() {
        do MemoryCustom.deAlloc(this);
        return;
    }
}
