class Main {
  function void main() {
  var int timestep;
    var Dino dino;
    var Cloud cloud;
    var Cactus cactus;
    let timestep = 50;
    let dino = Dino.new(50,150,-15,1);
    let cloud = Cloud.new(390,20,1, 5);
    let cactus = Cactus.new(400,150,3);

   
    while (true) {
      do cactus.update();
      do dino.update();
      do cloud.update();
      do Sys.wait(timestep);
    }
    return;
  }


}